package acctmw

// TODO: not a middleware, but helpers to associate arbitrary data with a
// session id (such as flash messages) and keep them associated when the
// session migrates from anonymous to authenticated. Should clear automatically
// when expired sessions get deleted.
